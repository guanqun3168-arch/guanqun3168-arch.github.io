# 事務所網站規則

本檔是給 AI 讀的專案規則。每次動工前先讀這份檔案。

事務所名稱：冠群建築師事務所

## 品牌

- 主色 #1A0E0A / 輔色 #B8985F / 底色 #FAF6EE
- 字型使用黑體，一律走系統內建字型堆疊（CSS 變數 --font-sans）：
  Windows 微軟正黑體、Mac 蘋方、Android 思源黑體。
  不得載入 Google Fonts 或任何外部字型檔。
- 標題字重 700，內文 400。不使用其他字重。
- 留白優先，不加陰影與漸層
  例外：首頁封面的文字遮罩，使用純色半透明（不得改成漸層）

## 內容規則

- 文字一律以 content/ 底下的檔案為準，不得杜撰。
- 沒有提供的資訊寫「待補」，不得自行填寫。
- 業主名稱一律不顯示。
- **案名一律不顯示，也不設個案頁面。**
- **不得指涉特定建物。** 不寫案名、不寫地點（縣市、行政區）、不寫主祀神明、
  不寫公司名或社區名。alt 文字與 meta 描述同樣適用。
  錯誤示範：「本案位於高雄市左營區，主祀開基太子爺。」
  正確示範：「既有廟宇的外觀改造，保留舊有內殿⋯」
- 作品頁不列地點、面積、年份、得獎等欄位，只放說明與圖說。

## 首頁精選作品

首頁的「精選作品」是**手動挑選**的清單，與 works/index.html 的完整列表
彼此獨立。在作品頁新增或移除案例時，不得自動同步到首頁。
要更動首頁精選，必須由使用者明確指定要放哪幾個案例。

## 作品呈現方式

固定四類，不得自行增減：

| 類別 | content 資料夾 |
| --- | --- |
| 寺廟設計 | content/works/temple/ |
| 廠房／辦公室 | content/works/industrial/ |
| 住宅設計 | content/works/residential/ |
| 其他建築 | content/works/other/ |

每一類在 works/index.html 是一個可展開的 `<details>`，內容為：

1. 一段設計觀點（來源：該類別的 說明.txt）
2. 一面照片牆（來源：該類別的 img/，經 tools/photo-style.py 處理）

照片檔名用「類別代號-流水號」，例如 temple-01.jpg。

## 首頁精選作品

首頁的「精選作品」是**獨立維護**的清單，由事務所自己挑選。

- 在 works/ 新增案例時，首頁**不得**自動加入，除非明確要求。
- 反之，從首頁移除某案例時，也不得刪掉 works/ 底下的個案頁。
- 建議維持三到六個。

## 技術規則

- 純靜態網站，不使用需要伺服器的功能。
- 手機 375px 為第一優先，桌機為次要。
- 圖片一律加 alt 文字。
- 不引入未經指定的第三方套件。
  已指定可用：聯絡頁的 Google 地圖 iframe（僅此一處，且必須加 loading="lazy"）。

## 修改規則

- 每次修改先說明會動到哪些檔案。
- 不得順手重構未被要求修改的檔案。

## 資料夾結構

```
studio-site/
├── index.html              首頁
├── works/                  作品案例
│   ├── index.html
│   └── [案名]/
├── about.html              事務所簡介
├── contact.html            聯絡方式
├── assets/
│   ├── css/                共用樣式
│   ├── js/                 共用腳本
│   └── img/                圖片（先壓縮）
├── content/                我提供的文字與圖片來源
├── AGENTS.md               給 AI 的事務所規則
└── README.md               給人看的說明
```

原則：

- 圖片一律用 tools/photo-style.py 處理後才放進 assets/img，
  確保全站色調一致。不得用其他方式手動調色或壓縮。
- 每個案子一個資料夾，命名沿用事務所自有的案件編碼系統。
  只要是半形英數與連字號即可，不使用中文。不得擅自更名。
- 不要把設計原始檔放進網站專案。
